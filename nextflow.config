/*
========================================================================================
    nf-wgs Nextflow Configuration
========================================================================================
    Main configuration file. Includes process-specific resource settings from
    conf/processes.config.
----------------------------------------------------------------------------------------
*/

// Include process-specific resource configuration
includeConfig 'conf/processes.config'

// Default parameters
params {

    // Input/output
    input           = null        // Path to samplesheet CSV (sample_id, fastq_1, fastq_2)
    outdir          = 'results'   // Output directory

    // Reference genome
    genome          = null        // Path to reference genome FASTA
    genome_index    = null        // Path to BWA-MEM2 index prefix (if pre-built)

    // GATK known variant sites (for BQSR)
    known_sites     = []          // List of VCF paths with known variant sites (e.g., dbSNP)
    known_sites_tbi = []          // Corresponding TBI index files

    // Variant filtration thresholds
    snp_filter_expr     = "QD < 2.0 || FS > 60.0 || MQ < 40.0 || MQRankSum < -12.5 || ReadPosRankSum < -8.0"
    indel_filter_expr   = "QD < 2.0 || FS > 200.0 || ReadPosRankSum < -20.0"

    // Maximum memory/cpus for a single process (used as upper bound)
    max_memory      = '128.GB'
    max_cpus        = 32
    max_time        = '240.h'

}

// Publish outputs to per-process subdirectories named after the process
process {
    publishDir = [
        path:    { "${params.outdir}/${task.process.tokenize(':')[-1].toLowerCase()}" },
        mode:    'copy',
        saveAs:  { filename -> filename.equals('versions.yml') ? null : filename }
    ]
}

// Enable DSL2
nextflow.enable.dsl = 2

// Docker/Singularity settings
docker {
    enabled    = false
    runOptions = '-u $(id -u):$(id -g)'
}

singularity {
    enabled    = false
    autoMounts = true
}

// Pipeline profiles
profiles {

    docker {
        docker.enabled      = true
        singularity.enabled = false
    }

    singularity {
        singularity.enabled = true
        docker.enabled      = false
    }

    test {
        params.input  = "${projectDir}/test/samplesheet.csv"
        params.outdir = 'test_results'
    }

}

// Manifest
manifest {
    name            = 'nf-wgs'
    author          = 'nf-wgs contributors'
    description     = 'Nextflow pipeline for whole-genome sequence data processing'
    nextflowVersion = '>=23.04.0'
    version         = '1.0.0'
}
